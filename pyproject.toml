[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "multimodal-restaurant-insights"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["pandas>=2.2", "pillow>=10"]

[project.optional-dependencies]
dev = ["jupyterlab>=4", "matplotlib>=3.8", "pytest>=8", "ruff>=0.6", "seaborn>=0.13"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
